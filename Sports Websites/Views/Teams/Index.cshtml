
@model List<Team>
<h1>Hello World</h1>
<h1>Teams</h1>

@* <table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>OfficialWebsiteURL</th>
            <th>Logo</th>
            <th>founadation date</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var team in Model)
        {
            <tr>
                <td>@team.Name</td>
                <td>@team.Description</td>
                <td>@team.OfficialWebsiteURL</td>
                <td>@team.Logo</td>
                <td>@team.FoundationDate</td>
                <td>
                    <button class="mr-2 btn btn-primary" asp-action="Edit" asp-route-id="@team.TeamID">Edit</button>
                    <button class="btn btn-dark" asp-action="Delete" asp-route-id="@team.TeamID">Delete</button>
                </td>
            </tr>
        }
    </tbody>
</table> *@
<div class="container row ">
@foreach (var team in Model)
{
    <div class="card col-3 m-4" style="width: 18rem;">
            <img src="@team.Logo" class="card-img-top" alt="..." />
        <div class="card-body">
            <h5 class="card-title">@team.Name</h5>
            <p class="card-text">@team.Description</p>
            <p class="card-text">@team.OfficialWebsiteURL</p>
            <p class="card-text">@team.FoundationDate</p>
            <a class="mr-2 btn btn-primary" asp-action="Edit" asp-route-id="@team.TeamID">Edit</a>
            <a class="btn btn-dark" asp-action="Delete" asp-route-id="@team.TeamID">Delete</a>
        </div>
    </div>     
}
</div>
<br><a class="btn btn-primary" style="text-align=center" asp-action="Create" >Create New Team</a>
